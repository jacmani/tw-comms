# apps/web

Placeholder. Per the Phase 1 scaffolding task, UI components are explicitly out of
scope until Phase 3 ("Dashboard — Compose, Templates, History"). This folder exists
now so the monorepo layout matches `01-specification.md` §3.1; run `pnpm create
svelte@latest` here when Phase 3 actually starts.
