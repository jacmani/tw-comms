node_modules/
dist/
build/
.env
.env.local
*.log

# Baileys auth session — NEVER commit this, it's equivalent to a live login session
apps/whatsapp-bot/auth_state/
apps/whatsapp-bot/auth_state_*/

# baileys-antiban warm-up/rate-limiter state — regenerable runtime state, not a secret,
# but it's per-deployment and shouldn't be committed
apps/whatsapp-bot/antiban-state*.json

# Pairing QR (src/index.ts writes this on each fresh pairing) — single-use and expires
# fast, but no reason to let it land in a commit either
apps/whatsapp-bot/qr.png

.DS_Store
.turbo/
